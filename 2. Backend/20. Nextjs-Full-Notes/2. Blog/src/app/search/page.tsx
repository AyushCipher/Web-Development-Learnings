// Placeholder route: "/search" is wired up in the file-system router
// (this file existing is enough to make the route respond) but not yet
// implemented - no data fetching, no searchParams reading, no UI. The
// Header component even has a commented-out spot reserved for a search
// input (see src/components/layout/header.tsx).
function SearchPage() {
  return <div>Search Page</div>;
}

export default SearchPage;
