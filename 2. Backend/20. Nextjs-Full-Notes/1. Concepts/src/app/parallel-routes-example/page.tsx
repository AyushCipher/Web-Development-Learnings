// This is the `children` slot for parallel-routes-example/layout.tsx - the
// "default" content of the route, same as `children` in any other layout.
export default function ParallelRoutesExample() {
  return <p>Main content (the `children` slot).</p>;
}
